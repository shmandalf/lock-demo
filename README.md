# Lock Demo - Визуализация распределенных семафоров

**Демонстрация работы распределенных семафоров на Redis для управления параллельной обработкой задач в Laravel приложениях.**

<div align="center">

![Статус](https://img.shields.io/badge/Статус-Активный-brightgreen)
![Laravel](https://img.shields.io/badge/Laravel-12.x-FF2D20?logo=laravel)
![PHP](https://img.shields.io/badge/PHP-8.5-777BB4?logo=php)
![CI/CD](https://github.com/shmandalf/lock-demo/actions/workflows/ci.yml/badge.svg)
![MIT](https://img.shields.io/badge/Лицензия-MIT-yellow)

*Интерактивная визуализация работы распределенных семафоров в реальном времени*

</div>

<video src="https://github.com/user-attachments/assets/b70c5d74-3836-470b-81e3-214c12a8bdcd" controls width="100%"></video>

## Описание проекта

Этот проект демонстрирует работу распределенных семафоров на Redis для контроля одновременного доступа к ограниченным ресурсам. Включает интерактивную панель, где можно наблюдать в реальном времени, как задачи соревнуются за блокировки, перемещаясь по анимированному конвейеру.

Визуализация показывает задачи, проходящие через различные этапы: **очередь → обработка → проверка блокировки → выполнение → завершение**. Это помогает понять, как распределенные системы управляют конкурентностью и ограничениями скорости.

**Новая возможность**: поддержка разных значений `maxConcurrent` для задач - каждая задача может иметь свой собственный лимит параллелизма, при этом задачи с разными значениями используют разные семафоры.

## Возможности

### Основные функции
- **Визуализация в реальном времени** с анимированным конвейером задач
- **Распределенные семафоры** на Redis для контроля конкурентности
- **Кастомизация лимитов** - пользователь может настраивать максимальное количество одновременно выполняемых задач
- **Поддержка различных лимитов параллелизма** - каждая задача может иметь свой `maxConcurrent` (1-10), задачи с разными значениями используют независимые семафоры
- **Live-обновления** через WebSocket (Soketi, совместим с Pusher)
- **Очередь RabbitMQ** для фоновой обработки задач
- **Автоматические повторные попытки** с экспоненциальной задержкой

### Визуальные особенности
- **Интерактивный пайплайн** с 5 секциями:
  - QUEUE - задачи в очереди
  - PROCESS - начало обработки
  - LOCK CHECK - проверка доступности семафора
  - IN PROGRESS - выполнение задачи
  - COMPLETE - завершение (задачи остаются видимыми)
- **Цветовая кодировка по `maxConcurrent`** - задачи отображаются разными цветами в зависимости от их лимита параллелизма (1-10)
- **Отображение значения `maxConcurrent`** - в каждом квадратике отображается цифра, соответствующая лимиту параллелизма для этой задачи
- **Адаптивный дизайн** с Tailwind CSS

### Новая функциональность v2.0
- **Индивидуальные лимиты параллелизма** - каждая задача создается с указанным значением `maxConcurrent`
- **Независимые семафоры** - задачи с разными `maxConcurrent` значениями используют разные семафоры (например: `semaphore:2`, `semaphore:5`, `semaphore:10`)
- **Визуальная идентификация** - цвет и цифра показывают лимит параллелизма каждой задачи
- **Гибкая настройка** - слайдер позволяет устанавливать `maxConcurrent` для новых задач

## Технологический стек

### Backend (моя специализация)
- **PHP 8.5** с современными возможностями
- **Laravel 12.x** - основной фреймворк
- **Redis** с Predis 3.3 - для распределенных семафоров и кэширования
- **RabbitMQ** - брокер сообщений для очередей
- **Laravel Queue RabbitMQ 14.4** - драйвер очереди
- **MySQL 8.4** - база данных
- **Pusher PHP Server 7.2** - WebSocket сервер (Soketi)
- **PHPUnit** - тестирование

### Frontend (реализовано с помощью DeepSeek 🤖)
- **Tailwind CSS** - утилитарный CSS фреймворк
- **Vanilla JavaScript** - нативный JS без фреймворков
- **Pusher.js** - клиентская библиотека для WebSocket
- **CSS3 анимации** - плавные переходы и движения
- **Адаптивный дизайн** - работа на всех устройствах

### Инфраструктура
- **Docker** с **Laravel Sail** - контейнеризация
- **Docker Compose** - оркестрация контейнеров

## 🎮 Демо и использование

### Интерфейс управления
1. **Настройка семафора для новых задач**:
   - Слайдер для выбора значения `maxConcurrent` (1-10)
   - Задачи создаются с указанным лимитом параллелизма

2. **Создание задач**:
   - "1 task" - создать одну задачу с текущим `maxConcurrent`
   - "5 tasks" - создать пять задач
   - "20 tasks" - создать двадцать задач
   - "50 tasks" - создать пятьдесят задач
   - "100 tasks" - создать сто задач

3. **Визуализация**:
   - Квадратики задач показывают их `maxConcurrent` значение (1-10)
   - Разные цвета для разных значений `maxConcurrent`
   - Задачи с одинаковым `maxConcurrent` конкурируют за один семафор
   - Задачи с разными `maxConcurrent` используют независимые семафоры

4. **Параметры выполнения**:
   - Semaphore acquire timeout: 3 секунды
   - Task processing time: 4 секунды
   - Semaphore TTL: 5 секунд

### Как работает демо
1. Пользователь выбирает значение `maxConcurrent` (по умолчанию 2)
2. Создает задачи с помощью кнопок - каждая задача получает выбранный лимит
3. Задачи отображаются с цветом и цифрой, соответствующими их `maxConcurrent`
4. Задачи перемещаются по пайплайну:
   - QUEUE → PROCESS → LOCK CHECK → IN PROGRESS → COMPLETE
5. В LOCK CHECK задачи проверяют доступность своего семафора:
   - Семафоры именуются как `semaphore:N`, где N = `maxConcurrent`
   - Например, задачи с `maxConcurrent=3` используют `semaphore:3`
6. Одновременно могут выполняться:
   - 2 задачи с `maxConcurrent=2` (семафор `semaphore:2`)
   - 5 задач с `maxConcurrent=5` (семафор `semaphore:5`)
   - 10 задач с `maxConcurrent=10` (семафор `semaphore:10`)
7. Завершенные задачи остаются в секции COMPLETE с пониженной прозрачностью

### 📊 Пример работы с разными семафорами

| # | Задача | maxConcurrent | Семафор | Слоты | Статус | Позиция |
|---|--------|---------------|---------|-------|--------|---------|
| 1 | 🟦 | 2 | `semaphore:2` | 🔴🟢 | ✅ Выполняется | IN PROGRESS |
| 2 | 🟦 | 2 | `semaphore:2` | 🔴🔴 | ✅ Выполняется | IN PROGRESS |
| 3 | 🟦 | 2 | `semaphore:2` | 🔴🔴 | ⏳ В очереди | QUEUE |
| 4 | 🟨 | 3 | `semaphore:3` | 🔴🟢🟢 | ✅ Выполняется | IN PROGRESS |
| 5 | 🟨 | 3 | `semaphore:3` | 🔴🔴🟢 | ✅ Выполняется | IN PROGRESS |
| 6 | 🟨 | 3 | `semaphore:3` | 🔴🔴🔴 | ✅ Выполняется | IN PROGRESS |
| 7 | 🟨 | 3 | `semaphore:3` | 🔴🔴🔴 | ⏳ В очереди | QUEUE |
| 8 | 🟨 | 3 | `semaphore:3` | 🔴🔴🔴 | ⏳ В очереди | QUEUE |

**📊 Легенда:**
- 🔴 = Занятый слот
- 🟢 = Свободный слот
- 🟦 = Задача с `maxConcurrent=2`
- 🟨 = Задача с `maxConcurrent=3`

**📈 Статистика:**
| Параметр | Семафор `semaphore:2` | Семафор `semaphore:3` | Всего |
|----------|----------------------|----------------------|-------|
| Лимит | 2 одновременных | 3 одновременных | 5 |
| Занято | 2 (100%) | 3 (100%) | 5 (100%) |
| В очереди | 1 задача | 2 задачи | 3 задачи |
| Всего задач | 3 | 5 | 8 |

**🎯 Что видно в интерфейсе:**
- **В IN PROGRESS:** 🟦🟦🟨🟨🟨 (5 задач)
- **В QUEUE:** 🟦🟨🟨 (3 задачи)
- **В логах:** Сообщения о занятых семафорах и ожидании

## Быстрый старт

### Требования
- Docker и Docker Compose
- Git
- Composer
- 4GB+ свободной памяти RAM

### Установка в один клик

```bash
# 1. Клонирование репозитория
git clone <repository-url>
cd lock-demo

# 2. Запуск автоматической установки
chmod +x setup.sh && ./setup.sh

# 3. Открыть в браузере
open http://localhost
```

### Настройка воркеров

**По умолчанию настроено 25 воркеров RabbitMQ**, что позволяет одновременно обрабатывать большое количество задач. Для изменения количества воркеров:

1. Отредактируйте файл `docker/8.5/supervisord.conf`:
```ini
numprocs=25  # Измените это значение
```

2. Пересоберите и перезапустите Docker-контейнеры:
```bash
# Останавливаем текущие контейнеры
./vendor/bin/sail down

# Пересобираем образ (Docker обнаружит изменения в конфиге)
./vendor/bin/sail build

# Запускаем контейнеры заново
./vendor/bin/sail up -d
```

3. Проверьте работу очередей:
```bash
# Проверить статус RabbitMQ (UI доступен на http://localhost:15672)
# Проверить выполнение задач через интерфейс демо
```

### Рекомендации по настройке воркеров

| Сценарий использования | Рекомендуемое количество воркеров | Описание |
|------------------------|-----------------------------------|----------|
| 🧪 **Тестирование / Разработка** | 5-10 воркеров | Оптимально для локальной разработки и тестирования |
| 🚀 **Продакшн (стандарт)** | 10-25 воркеров | Для рабочих сред со средней нагрузкой |
| 🔥 **Высокая нагрузка** | 25-50 воркеров | Для систем с интенсивным потоком задач |
| 🎯 **Демонстрация Lock Demo** | 25 воркеров | Значение по умолчанию для демонстрации работы семафоров |

**⚠️ Важно:** Поскольку файл `supervisord.conf` копируется в Docker-образ при сборке, любые изменения требуют пересборки контейнеров командой `./vendor/bin/sail build`.

## Доступные сервисы
После установки будут доступны:

| Сервис | URL | Credentials |
|--------|-----|-------------|
| Приложение | http://localhost | — |
| RabbitMQ UI | http://localhost:15672 | `admin` / `admin` |
| База данных | localhost:3306 | `sail` / `password` |
| Redis | localhost:6379 | — |